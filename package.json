{
	"name": "WP25EasterEggs",
	"private": true,
	"scripts": {
		"lint": "npm -s run lint:styles && npm -s run lint:js && npm -s run lint:i18n",
		"lint:fix": "npm -s run lint:styles -- --fix && npm -s run lint:js -- --fix && npm -s run lint:i18n",
		"lint:js": "eslint --cache .",
		"lint:styles": "stylelint --cache --report-needless-disables --allow-empty-input **/*.{css,less}",
		"lint:i18n": "banana-checker --requireLowerCase=0 i18n/",
		"test": "npm run lint && npm run test:unit",
		"test:unit": "jest --silent --passWithNoTests"
	},
	"devDependencies": {
		"@types/jest": "^30.0.0",
		"@wikimedia/mw-node-qunit": "^7.2.0",
		"eslint-config-wikimedia": "0.32.3",
		"grunt-banana-checker": "0.13.0",
		"jest": "^30.2.0",
		"jest-environment-jsdom": "^30.2.0",
		"stylelint-config-wikimedia": "0.18.0",
		"types-mediawiki": "^2.0.0"
	}
}
